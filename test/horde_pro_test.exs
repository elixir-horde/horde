defmodule HordeProTest do
  use ExUnit.Case
  doctest HordePro

  # alias HordeProd.DynamicSupervisor, as: Sup

  # test "starts a child" do
  #   Sup.start_link(name: :n1, strategy: :one_for_one, repo: HordeProTest.Repo)
  # end
end
